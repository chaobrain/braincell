# SC_channel

`single_compartment_channel` 家族目录。

- `specs/`: 单 compartment channel 对比设计说明
- `cases/`: 单 case 与 `hh_fixed_ion` sweep 样例
- `templates/`: `SC_channel` 入口与 `hh_fixed_ion` 具体模板
- `artifacts/`: `hh_fixed_ion` 现有运行产物
- `notebooks/`: `hh_fixed_ion` 预览 notebook
