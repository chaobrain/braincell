# Package marker for the cable notebook workflow helpers.
#
# This lets `workflow_api` import its engine siblings as `cable.engine.*`
# instead of bare `experiment_schema` / `run` / `outputs`, which the sibling
# `channel_no_conc` suite also defines.
